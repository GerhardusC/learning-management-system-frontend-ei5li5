import Register from "@/components/landing/Register";
import React from "react";
// The page that returns the registration component.
const RegisterPage = () => {
  return (
    <div>
      <Register />
    </div>
  );
};

export default RegisterPage;
